# Progress — [Project Name]

**Rule:** Exactly 5 lines per entry, written at every slice boundary BEFORE `/compact`. Newest entry on top. This is the context-reset anchor — Claude re-reads the top entry + `DECISIONS_LOG.md` before starting the next slice.

---

## YYYY-MM-DD — after Slice N

1. **Slices done:** [list]
2. **Current state:** [what works right now, deployed where]
3. **Next slice:** [number + capability + its riskiest assumption]
4. **Open decisions:** [anything DEFERRED or awaiting Gaurav]
5. **Kill criterion check:** [days since start vs budget — OK / TRIGGERED]
