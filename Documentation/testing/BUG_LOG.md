# Bug Log — Household Financial Planning PWA

**Rule:** Append-only. Every bug gets a root cause and a regression test — "fixed it" without either is not closed. Lessons that generalize go to Past Mistakes in memory.
**Gate condition:** Zero open P0/P1 before deployment.

Severity: **P0** data loss / security / payments broken · **P1** core journey broken · **P2** everything else.

---

| ID | Date | Severity | Summary | Reproduction steps | Root cause | Fix commit | Regression test | Status |
|---|---|---|---|---|---|---|---|---|
| B-004 | 2026-07-28 | P2 (test-suite integrity) | Flaky test: `Dashboard.test.tsx` asserts analytics calls that a `useEffect` has not fired yet — intermittently fails with `Number of calls: 0` | Run `npx vitest run src/pages/Dashboard.test.tsx` repeatedly. Roughly 1 run in 8 fails on "fires nudge_shown once per load with the selected check" (and the sibling `dashboard_viewed` test shares the defect) with `AssertionError: expected "spy" to be called ... Number of calls: 0`. | Both `track()` calls live in a `useEffect` in `src/pages/Dashboard.tsx:75`, which React commits **after** the DOM mutation that `screen.findByText()` resolves on. The tests asserted on the spy immediately after `findByText` returned, so they assumed "the text is on screen" implies "the effect has run". It usually does; under load it does not. **This is the same failure recorded as unreproduced on 2026-07-19** ("2 failures in 1 of 7 runs that day, never reproduced; `vi.restoreAllMocks()` leak the plausible-but-unconfirmed cause"). The mock-leak hypothesis was wrong — the mocks were fine, the assertion was simply early. It stayed unreproduced because a single local run passes ~7 times out of 8. **Adding CI is what caught it**: the very first workflow run on `main` failed, on a suite that had passed locally minutes earlier. | `(this commit)` | `src/pages/Dashboard.test.tsx` — both analytics assertions now wrapped in `waitFor(...)`, so they wait for the effect instead of racing it. Verified 12/12 clean runs after the fix, against 1 failure in 8 before. | closed |
| B-003 | 2026-07-28 | **NOT A BUG — closed** | Remove-member confirm dialog opens once, then will not reopen | On `/profile` with 2 members, tap "Remove" on the second → dialog opens (confirmed, copy correct). Tap "Keep them" → closes correctly, member retained. Tap "Remove" again → **dialog does not open**, and has not opened on any subsequent attempt. | Not diagnosed, and **may not be a defect at all.** Argues for a real bug: it survived a full page reload; it fails via mouse *and* via keyboard (focus + Enter); `document.elementFromPoint(615,408)` returns the correct `BUTTON` with text "Remove", so the input is reaching the intended element with nothing overlaying it; `devicePixelRatio` 1 and `scrollY` 0 rule out coordinate skew; console shows no errors beyond the known Clerk dev-key warning. Argues for tooling flake: the same browser session produced repeated `Page.captureScreenshot` CDP timeouts and one full extension disconnect, and other Radix triggers in this same run needed two clicks to activate. **Resolved 2026-07-28: automation flake, not a defect.** Gaurav ran the discriminator — the dialog opened for a human on demand and he removed both members successfully. Root cause is on the automation side (this session logged repeated `Page.captureScreenshot` CDP timeouts, one extension disconnect, and several Radix triggers needing two synthetic clicks); the app is fine. **Lesson kept:** `elementFromPoint` returning the right element proves the *coordinate* is right, not that the synthetic event will activate a React/Radix handler — those are different claims, and I treated the first as evidence for the second. When a control works once and then stops under automation, suspect the driver before the app, and get a human to press it before writing a defect. | n/a — no defect | n/a | **closed** |
| B-002 | 2026-07-28 | P2 | Add-holding sheet leaves the page scrolled past the end of its own content — user lands on a blank viewport after adding a holding | On `/portfolio` with 1 holding, tap "+", fill the form (the sheet is taller than the viewport, so the page scrolls down), tap "Add to plan". Sheet closes; the holdings list is short, so the retained scroll offset leaves nothing but the FAB visible. Must scroll up manually to see the holding just added. | `src/pages/Portfolio.tsx`'s Sheet (`SheetContent side="bottom"`, `src/components/ui/sheet.tsx`) is `position: fixed` and taller than the viewport. Mobile browsers scroll the underlying *document* — not the fixed sheet — to bring a focused field above the keyboard, so `window.scrollY` drifts upward while the sheet is open. Sheet close (`handleSaved` and the Sheet's `onOpenChange`) never reset it, so the page kept whatever offset the tall sheet had reached. Shared seam: the edit-holding sheet goes through the same close path and was equally affected — it just didn't reproduce visually in the live pass because its scroll depth was shorter. | `efb2253` | `src/pages/Portfolio.test.tsx` — "restores the pre-open scroll position after the add-holding sheet closes (B-002)" + "…after the edit-holding sheet closes (B-002, shared seam)"; both RED before fix (`window.scrollTo` never called) | closed |
| B-001 | 2026-07-21 | P1 | Slice 7 nudge card never renders in production — SPEC.md §7 "exactly one nudge, never zero" violated live | Sign in with a household that has ≥1 unmet check, open `/dashboard` — no "Next step" card appears (donut → install card, nothing between) | `server/routes/dashboard.ts` hand-picked response fields and omitted `nudge`; `getDashboard()` computed it correctly but the route dropped it on serialization. Frontend degrades silently when `nudge` is absent (`Dashboard.tsx:169`), so no crash/console error masked it. All 294 tests passed because the route→response seam was never asserted (getDashboard tested to return nudge; NudgeCard tested fed one directly; the gap between them untested). | `6e8ff4e` | `server/dashboard.integration.test.ts` — "always forwards a nudge in the response — never zero" (0 members → member_coverage) + nudge assertion on the full-coverage fixture (→ complete); both RED before fix | closed |
